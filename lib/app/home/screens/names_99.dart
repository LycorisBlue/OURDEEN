import '/constants/app_export.dart';

class Name99Screen extends StatelessWidget {
  const Name99Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return MyAppScaffold(
        appBar: AppBar(
          title: Text("Les 99 Noms d'AllAh "),
        ),
        body: const Placeholder());
  }
}
