import 'package:flutter/material.dart';

class CoranTab extends StatelessWidget {
  const CoranTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Coran"),
    );
  }
}
